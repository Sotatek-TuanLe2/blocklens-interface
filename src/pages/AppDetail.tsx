import React from 'react';
import { AppButton } from 'src/components';
import BasePage from 'src/layouts/BasePage';

const AppDetail = () => {
  return (
    <BasePage>
      <AppButton>Delete App</AppButton>
    </BasePage>
  );
};

export default AppDetail;
